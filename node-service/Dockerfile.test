# Test Dockerfile for Node.js service
FROM node:18-alpine

WORKDIR /app

# Copy package.json and install dependencies
COPY package.json ./
RUN npm install -g jest && npm install --prefer-offline --no-audit --include=dev

# Copy the rest of the application
COPY . ./

# Run tests
CMD ["npm", "test"]